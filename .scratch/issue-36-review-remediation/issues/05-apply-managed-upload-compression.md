# 05 — 落实 managed 自动上传载荷压缩

**What to build:** managed 粘贴后的自动上传遵循独立的上传载荷压缩设置；本地未压缩时可压缩上传副本，本地已经按相同质量压缩时直接复用结果，避免重复有损压缩。

**Blocked by:** None — can start immediately

**Status:** completed

- [x] 关闭本地粘贴压缩、开启上传压缩时，本地附件保持原始内容而上传载荷按设置压缩。
- [x] 本地粘贴和上传压缩均开启且质量相同时，上传复用已生成的数据，不执行第二次有损压缩。
- [x] 仅开启本地粘贴压缩或两项均关闭时，上传载荷与已确认策略一致。
- [x] 当前图片、笔记、批量和 delegated 上传的既有压缩行为保持不变，并有回归测试。
