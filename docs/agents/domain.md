# Domain docs

Before exploring, read root `CONTEXT.md` and relevant ADRs under `docs/adr/` when they exist. If absent, proceed silently.

This is a single-context repository. Use terminology defined in `CONTEXT.md`, and explicitly surface any conflict with an existing ADR.
